extends ../layouts/base

block js
    script(src='js/vendor/xdate.js')
    script(src='js/vendor/underscore.js')
    script(src='js/vendor/backbone.js')
    script(src='js/vendor/Chart.js')
    script(src='js/main.js')

block sidebar
    .controls
        h3 Select request type

        .switch.three.small.typeControls

        h3 Select duration

        .switch.four.small.durationControls

block content
    //.switch.big
        input#graph(name='view', type='radio', checked='checked', data-caro='prev')
        label.graphicon(for='graph', onclick='')

        input#map(name='view', type='radio', data-caro='next')
        label.mapicon(for='map', onclick='')

        span.slide-button

    .slides
        .charts
            .latencies.row
                .chartContainer.columns
                .uptimeContainer
                    .info
                        h2
                            abbr(title='Uptime in percentage. The higher, the better.') Uptime
                .latencyContainer
                    .info
                        h2
                            abbr(title='Latency is given as average for the defined range of time. The lower, the better.') Latency
        //.map
            img(src='images/worldmap.svg')
            p TODO

    .legendContainer.columns
